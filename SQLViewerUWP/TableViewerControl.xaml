<UserControl x:Class="SQLViewerUWP.TableViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top toolbar -->
        <Grid Grid.Row="0" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="LblServer" Text="服务:" 
                       VerticalAlignment="Center" Margin="0,0,10,0"/>

            <TextBox Grid.Column="1" x:Name="TxtCondition" Height="30" 
                     FontSize="12" VerticalContentAlignment="Center"
                     KeyDown="TxtCondition_KeyDown"/>

            <ListBox Grid.Column="1" x:Name="LstSuggestions" 
                     Height="150" MaxWidth="300"
                     Visibility="Collapsed"
                     VerticalAlignment="Top"
                     MouseDoubleClick="LstSuggestions_MouseDoubleClick"
                     KeyDown="LstSuggestions_KeyDown"/>

            <Button Grid.Column="2" x:Name="BtnQuery" Content="查询" 
                    Width="80" Height="30" Margin="5,0,0,0"
                    Click="BtnQuery_Click"/>

            <Button Grid.Column="3" x:Name="BtnReset" Content="重置排序" 
                    Width="80" Height="30" Margin="5,0,0,0"
                    Click="BtnReset_Click"/>

            <Button Grid.Column="4" x:Name="BtnCount" Content="计算总数" 
                    Width="80" Height="30" Margin="5,0,0,0"
                    Click="BtnCount_Click"/>

            <Button Grid.Column="5" x:Name="BtnToggleDDL" Content="表结构" 
                    Width="80" Height="30" Margin="5,0,0,0"
                    Click="BtnToggleDDL_Click"/>
        </Grid>

        <!-- Info panel -->
        <Grid Grid.Row="1" Margin="5,0,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="LblSql" Text="" 
                       FontSize="10" Foreground="Gray" 
                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

            <Button Grid.Column="1" Content="复制SQL" Width="60" Height="25" 
                    FontSize="10" Click="BtnCopySql_Click" Margin="5,0,0,0"/>

            <TextBlock Grid.Column="2" x:Name="LblQueryTime" Text="查询时间：" 
                       FontSize="11" Margin="10,0,0,0" VerticalAlignment="Center"/>

            <TextBlock Grid.Column="3" x:Name="LblCount" Text="条数：" 
                       FontSize="11" Margin="10,0,0,0" VerticalAlignment="Center"/>

            <TextBlock Grid.Column="4" x:Name="LblTotal" Text="总数：" 
                       FontSize="11" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </Grid>

        <!-- Main content area -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- DataGrid -->
            <DataGrid Grid.Row="0" x:Name="DgData" 
                      AutoGenerateColumns="True"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="All"
                      FontSize="12"
                      RowHeight="30"
                      ColumnHeaderHeight="36"
                      SelectionMode="Extended"
                      SelectionUnit="Cell"
                      Sorting="DgData_Sorting"/>

            <!-- Table DDL viewer (collapsible) -->
            <TextBox Grid.Row="1" x:Name="TxtDDL" 
                     Height="200"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     IsReadOnly="True"
                     FontFamily="Consolas"
                     FontSize="11"
                     Visibility="Collapsed"/>
        </Grid>
    </Grid>
</UserControl>
